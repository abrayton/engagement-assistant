{
  "name": "engagement-assistant",
  "version": "1.0.0",
  "description": "Local engagement assistant — poll, score, draft, review, copy.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.95.1",
    "better-sqlite3": "^12.9.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "node-cron": "^4.2.1",
    "snoowrap": "^1.15.2"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
